{
  "name": "@claude-blocker/extension",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/service-worker.ts src/content-script.ts src/popup.ts src/options.ts --format iife --out-dir dist --clean && node scripts/generate-icons.mjs && node scripts/copy-assets.mjs",
    "dev": "pnpm build --watch",
    "typecheck": "tsc --noEmit",
    "pack": "node scripts/pack-crx.mjs",
    "zip": "cd dist && zip -r ../claude-blocker.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "crx": "^5.0.1",
    "sharp": "^0.34.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
